SELECT
  channelGrouping,
  ARRAY_AGG(STRUCT(geoNetwork_country,
      date,
      totals_transactions)
  ORDER BY
    date ASC) AS grouping_
FROM
  `data-to-insights.ecommerce.rev_transactions`
GROUP BY
  channelGrouping